#include "main.h"

/**
 * mul - Afunction that multiplies two integers
 * @a: function parameter
 * @b: function parameter
 * Return: Always 0
 */

int mul(int a, int b)
{
	int multiply;

	multiply = a * b;
	return (multiply);
}
